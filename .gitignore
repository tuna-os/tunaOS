cosign.key
output/
*.raw
*.qcow2
.rpm-cache
.rpm-cache*
.rechunk/
.build-logs/
.build/
# Temporary build artifacts
just.tar.gz
Cargo.lock
Cargo.toml
GRAMMAR.md
just.1
completions/

# dev files
hack/

secrets.env
build.log
out.ociarchive
*.iso
*.qcow2
.lima-home/

# Local tooling / agent state (not project content). The Spektacular skills
# are project content (RFC 011): every Claude session needs them to follow
# the spec, plan and build workflows.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/spek-*/
.antigravitycli/
.pi/
.gemini/

# PI Coding Agent session logs (terminal recording dumps)
tuna.txt
ubuntu.txt
.pi-subagents/

# Python bytecode
__pycache__/
*.py[cod]

# QEMU e2e evidence directories (scripts/iso-e2e.sh --output). Serial logs,
# screenshots, install disks and swtpm state: local run output, never source.
iso-e2e-out/
luks-out/
live-lab-out/
*-e2e-out/
*-dbg-out/
*-ready-out/

# Transient scratch from tests/bats/test_verify_image_packages.bats. Its
# teardown removes it, so it exists only while the suite runs — long enough
# for a concurrent `git add -A` to commit it, which is how it got in once.
tests/bats/test_root_tmp/
