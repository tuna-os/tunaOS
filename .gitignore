cosign.key
output/
*.raw
*.qcow2
.rpm-cache
.rpm-cache*
.rechunk/
.build-logs/
.build/
# Temporary build artifacts
just.tar.gz
Cargo.lock
Cargo.toml
GRAMMAR.md
just.1
completions/

# dev files
hack/

secrets.env
build.log
out.ociarchive
*.iso
*.qcow2
.lima-home/

# Local tooling / agent state (not project content)
.claude/
.antigravitycli/
.pi/
.gemini/

# PI Coding Agent session logs (terminal recording dumps)
tuna.txt
ubuntu.txt
.pi-subagents/

# Python bytecode
__pycache__/
*.py[cod]

# QEMU e2e evidence directories (scripts/iso-e2e.sh --output). Serial logs,
# screenshots, install disks and swtpm state: local run output, never source.
iso-e2e-out/
luks-out/
live-lab-out/
*-e2e-out/
