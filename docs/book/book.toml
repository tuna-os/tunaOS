[book]
authors = ["TunaOS Team"]
language = "en"
src = "src"
title = "TunaOS Documentation"

# ⚠️  This mdBook is deprecated. The canonical developer documentation
# lives at https://tunaos.org — the Docusaurus site syncs these source
# files into its Developer Guide via CI.
#
# The source files (docs/book/src/*.md) remain the single source of
# truth and are kept here so they live alongside the code they document.
# Do not delete them — the sync script in tuna-os/docs pulls from here.
#
# The GitHub Pages deploy (deploy-docs.yml) was removed 2026-06-13.

[output.html]
git-repository-url = "https://github.com/tuna-os/tunaOS"
