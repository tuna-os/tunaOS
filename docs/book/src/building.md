# Building TunaOS

TunaOS is built using `podman` and the `just` command runner.

## Quick Start

```bash
just build yellowfin gnome
```

This will build the Yellowfin variant with the GNOME desktop environment locally.
