# Security: No default password. Require SSH key injection at build time.
# See Justfile qcow2 recipe for the key injection mechanism.
[[customizations.user]]
name = "centos"
password = null
groups = ["wheel"]

[[customizations.filesystem]]
mountpoint = "/"
minsize = "20 GiB"
