# Security: No default password. Generate one at build time or use SSH keys.
# [[customizations.user]]
# name = "centos"
# password = "centos"  # REMOVED: hardcoded default credential
# groups = ["wheel"]

[[customizations.filesystem]]
mountpoint = "/"
minsize = "20 GiB"
