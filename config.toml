# Security: No default password. Generate one at build time or use SSH keys.
# [[user]]
# name = "admin"
# password = "password"  # REMOVED: hardcoded default credential
# groups = ["wheel"]
# ssh_authorized_keys = []
