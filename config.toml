# Security: No default password. Require SSH keys at build time.
# See Justfile qcow2 recipe for key injection mechanism.
[[user]]
name = "admin"
password = null
groups = ["wheel"]
ssh_authorized_keys = [] # REQUIRED: Populate with SSH public keys before use
