config:
  global_platforms: ["linux/amd64", "linux/arm64"]

# ── Grouped dedup ISOs (issue #455) ───────────────────────────────────────────
# Per-desktop-per-flavor ISOs are wasteful: all desktops on a variant share the
# same Enterprise-Linux base, so tacklebox `shared_store.dedup` packs several
# bootable environments into ONE combined squashfs (~80%+ dedup; an extra
# desktop adds ~300 MB rather than a whole new ISO). Each group below becomes a
# single ISO named `<variant>[-<suffix>].iso` whose systemd-boot menu lists
# every environment by title.
#
# `scripts/build-iso-group.sh` intersects each group's `flavors` with the
# variant's actual `build_image: true` flavors, so a variant that lacks a
# Variants that lack certain desktops simply get a smaller ISO — no group
# needs per-variant tailoring here.
iso_groups:
  # Desktop ISO group: boots GNOME NVIDIA by default, and embeds
  # both NVIDIA and non-NVIDIA flavors in the offline store for installer auto-detection.
  # Published as `<variant>.iso`.
  - suffix: ""
    flavors: [gnome-nvidia, gnome-nvidia-hwe]
    offline_flavors: [gnome, gnome-hwe, gnome-nvidia, gnome-nvidia-hwe]
  # ONE ISO PER BASE+DESKTOP, each embedding its own NVIDIA and HWE flavors —
  # the same shape as the GNOME group above, applied to the other four
  # desktops. Published as `<variant>-<suffix>.iso`.
  #
  # This replaces a single `community` group that packed KDE, COSMIC, Niri and
  # XFCE into ONE ISO and carried `publish: false`, on the reasoning that the
  # browser ISO Builder covers the long tail on demand so a five-flavor
  # offline store was not worth the storage. The cost argument was sound; the
  # conclusion was not. A user who wants KDE should get a KDE ISO, not a
  # four-desktop ISO or a browser build step, and one desktop's offline store
  # is a fraction of four.
  #
  # Storage does not grow the way the old comment implied either: these are
  # DEDUPLICATED groups over a shared squashfs, so a per-desktop ISO carries
  # its own desktop plus the base layers it already shares with every other
  # ISO of the same variant.
  - suffix: kde
    flavors: [kde-nvidia]
    offline_flavors: [kde, kde-hwe, kde-nvidia]
  - suffix: cosmic
    flavors: [cosmic-nvidia]
    offline_flavors: [cosmic, cosmic-hwe, cosmic-nvidia]
  - suffix: niri
    flavors: [niri-nvidia]
    offline_flavors: [niri, niri-hwe, niri-nvidia]
  - suffix: xfce
    flavors: [xfce-nvidia]
    offline_flavors: [xfce, xfce-hwe, xfce-nvidia]

# ── Sibling images ─────────────────────────────────────────────────────────
# TunaOS-family bootc images built in their OWN repositories with BuildStream
# on freedesktop-sdk. They are whole-OS products, not package sources, and
# they never enter the variant matrix below or green-criteria.yml: each repo
# runs its own build, live-ISO, plain-install and LUKS-install checks.
# .github/scripts/sibling-images-status.sh renders their latest main-branch
# build into the README next to the variant matrix so the family is visible
# in one place. `workflow` is the repo's SCHEDULED build workflow.
sibling_images:
  - id: tromso
    emoji: "🏔️"
    description: "Aurora Tromsø: KDE Plasma 6 on freedesktop-sdk (BuildStream)"
    image: ghcr.io/tuna-os/tromso
    repo: tuna-os/tromso
    workflow: build-tromso-multirunner.yml
    desktops: [KDE]
  - id: xfce-linux
    emoji: "🐭"
    description: "XFCE Linux: XFCE Wayland on freedesktop-sdk (BuildStream)"
    image: ghcr.io/tuna-os/xfce-linux
    repo: tuna-os/xfce-linux
    workflow: build-multirunner.yml
    desktops: [XFCE]

# Build stages (for reference — enforced by the workflow job graph):
#
#   Stage 1:  base
#                ├── Stage 2: base-hwe, base-nvidia
#                └── Stage 2: gnome, kde, niri
#                                ├── Stage 3: gnome-hwe, kde-hwe, niri-hwe   (layer on base-hwe)
#                                └── Stage 3: gnome-nvidia, kde-nvidia, niri-nvidia   (layer on base-nvidia)
#                                                └── Stage 4: gnome-nvidia-hwe  (layer on gnome-hwe)
#
# The `stage` field on each flavor tells generate_matrix which job matrix it belongs to:
#   stage: 1  → build_base      (no dependencies)
#   stage: 2  → build_stage2    (needs: build_base)
#   stage: 3  → build_stage3    (needs: build_stage2)
#   stage: 4  → build_stage4    (needs: build_stage3)
variants:
  - id: yellowfin
    emoji: "🐠"
    description: "Based on AlmaLinux Kitten 10"
    base_image: "quay.io/almalinuxorg/almalinux-bootc:10-kitten@sha256:95d73b4e20fd6562593272e8b1630b9376b4a34507ed10bdf5f56ae10f7d7438"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [almalinux-kitten]
    platforms: ["linux/amd64", "linux/amd64/v2", "linux/arm64"]
    flavors:
      # ── Stage 1 ────────────────────────────────────────────────────────────
      - id: base
        stage: 1
        build_image: true
        build_iso: false
        build_qcow2: false

      # ── Stage 2 ────────────────────────────────────────────────────────────
      - id: base-hwe
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: base-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: true
      - id: cosmic
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false

      - id: kde
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
        # tunaOS xfce-wayland stack comes from repo.tunaos.org, which only
        # publishes EL10 x86_64 packages — no aarch64 until the repo grows one.
        platforms:
          - linux/amd64
          - linux/amd64/v2

      # ── Stage 3 ────────────────────────────────────────────────────────────
      # HWE: no ISO — install the standard ISO then `bootc switch` to *-hwe
      - id: gnome-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      # Apple Silicon (M1/M2, Asahi Linux) — overlay/asahi.sh, CentOS
      # Hyperscale SIG packages-asahi (EXPERIMENTAL: kernel + glue only; boot
      # payloads pending #777). aarch64 only; no ISO (asahi-installer path).
      - id: gnome-asahi
        stage: 3
        platforms: ["linux/arm64"]
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: gnome-nvidia-hwe
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 4
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: cosmic-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: cosmic-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: kde-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: niri-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
        # tunaOS xfce-wayland stack comes from repo.tunaos.org, which only
        # publishes EL10 x86_64 packages — no aarch64 until the repo grows one.
        platforms:
          - linux/amd64
          - linux/amd64/v2
      - id: xfce-nvidia
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
        # x86_64 only. The xfce-wayland repo constraint (no aarch64) applied
        # already; the nvidia overlay narrows further — no amd64/v2, because
        # Kitten v2 kernels are ARCH=x86_64_v2 and the akmods kmods are
        # x86_64 builds, so the exact-EVR kmod install can never match there
        # (overrides/nvidia/10-kernel-swap.sh hard-fails off x86_64).
        platforms: ["linux/amd64"]

  - id: albacore
    emoji: "🐟"
    description: "Based on AlmaLinux 10"
    base_image: "quay.io/almalinuxorg/almalinux-bootc:10@sha256:5a1c306cd49c33294a70ef542538fa2bca7b2a905471b89b1f40e123e9370ac5"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [almalinux]
    platforms: ["linux/amd64", "linux/amd64/v2", "linux/arm64"]
    flavors:
      # ── Stage 1 ────────────────────────────────────────────────────────────
      - id: base
        stage: 1
        build_image: true
        build_iso: false
        build_qcow2: false

      # ── Stage 2 ────────────────────────────────────────────────────────────
      - id: base-hwe
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: base-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: true

      - id: cosmic
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
        # tunaOS xfce-wayland stack comes from repo.tunaos.org, which only
        # publishes EL10 x86_64 packages — no aarch64 until the repo grows one.
        platforms:
          - linux/amd64
          - linux/amd64/v2

      # ── Stage 3 ────────────────────────────────────────────────────────────
      # HWE: no ISO — install the standard ISO then `bootc switch` to *-hwe
      - id: gnome-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      # Apple Silicon (M1/M2, Asahi Linux) — overlay/asahi.sh, CentOS
      # Hyperscale SIG packages-asahi (EXPERIMENTAL: kernel + glue only; boot
      # payloads pending #777). aarch64 only; no ISO (asahi-installer path).
      - id: gnome-asahi
        stage: 3
        platforms: ["linux/arm64"]
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: gnome-nvidia-hwe
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 4
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: cosmic-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: cosmic-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: kde-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: niri-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
        # tunaOS xfce-wayland stack comes from repo.tunaos.org, which only
        # publishes EL10 x86_64 packages — no aarch64 until the repo grows one.
        platforms:
          - linux/amd64
          - linux/amd64/v2
      - id: xfce-nvidia
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
        # x86_64 only. The xfce-wayland repo constraint (no aarch64) applied
        # already; the nvidia overlay narrows further — no amd64/v2, because
        # Kitten v2 kernels are ARCH=x86_64_v2 and the akmods kmods are
        # x86_64 builds, so the exact-EVR kmod install can never match there
        # (overrides/nvidia/10-kernel-swap.sh hard-fails off x86_64).
        platforms: ["linux/amd64"]

  - id: skipjack
    emoji: "🍣"
    description: "Based on CentOS Stream 10"
    base_image: "quay.io/centos-bootc/centos-bootc:stream10@sha256:2b7e3b1abf8db094d1efb083721dc0f72e6feeef2355fc16ea010d0266b2bb95"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [centos]
    # Note: linux/amd64/v2 intentionally omitted — verify builder support before adding.
    platforms: ["linux/amd64", "linux/arm64"]
    flavors:
      # ── Stage 1 ────────────────────────────────────────────────────────────
      - id: base
        stage: 1
        build_image: true
        build_iso: false
        build_qcow2: false

      # ── Stage 2 ────────────────────────────────────────────────────────────
      - id: base-hwe
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: base-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: true
      - id: cosmic
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false

      - id: kde
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
        # tunaOS xfce-wayland stack comes from repo.tunaos.org, which only
        # publishes EL10 x86_64 packages — no aarch64 until the repo grows one.
        platforms:
          - linux/amd64

      # ── Stage 3 ────────────────────────────────────────────────────────────
      # HWE: no ISO — install the standard ISO then `bootc switch` to *-hwe
      - id: gnome-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      # Apple Silicon (M1/M2, Asahi Linux) — overlay/asahi.sh, CentOS
      # Hyperscale SIG packages-asahi (EXPERIMENTAL: kernel + glue only; boot
      # payloads pending #777). aarch64 only; no ISO (asahi-installer path).
      - id: gnome-asahi
        stage: 3
        platforms: ["linux/arm64"]
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      # gnome-nvidia-hwe re-enabled (tunaOS#639): the gnome-shell-49 vs
      # gnome-shell-common-48 file conflict is fixed upstream in the spec
      # (tuna-os/tunaos-packages#23 merged: Obsoletes: gnome-shell-common <
      # %{major_version}), verified live in the published spec, not just
      # by the PR having merged.
      - id: gnome-nvidia-hwe
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 4
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: cosmic-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: cosmic-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: kde-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: niri-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false


  # ── Wahoo ────────────────────────────────────────────────────────────────
  # Fedora ELN — the EL11 early-warning lane.
  #
  # ELN ("Extra Lifecycle Notes") is Fedora Rawhide's sources built with
  # Enterprise Linux macros, buildroot and repo layout. Its os-release is
  # already the thing this variant exists to preview:
  #
  #   ID=eln  VERSION_ID=11  ID_LIKE="rhel centos fedora"  VARIANT_ID=eln
  #
  # — measured on the pinned digest below, 2026-08-25. That is the same 11
  # that c10s→c11s and almalinux-bootc:11-kitten will carry, months before
  # either exists to build against. Yellowfin (Kitten 10), Albacore (Alma 10)
  # and Skipjack (c10s) are all EL10; nothing in the matrix sees EL11 today,
  # so an EL11 break surfaces the day a base flips, not before. That is the
  # gap this fills, and it is why the variant is EL-family rather than a
  # bonito flavor: ELN is upstream of every EL11 rebuild, not of Fedora 45.
  #
  # Upstream base availability (VARIANT-LIFECYCLE.md admission gate #3):
  # registry.fedoraproject.org/eln-bootc is the image fedora-eln/eln#214
  # asked for, and it exists — an OCI index carrying amd64, arm64, ppc64le
  # and s390x, 480 packages, bootc-1.16.7, dnf5-5.4.3.0, kernel-7.3.0-rc.
  # Both declared platforms are satisfied by the index AND by the desktop
  # payload: gnome-shell/gdm/mutter/nautilus/gnome-control-center/
  # mesa-dri-drivers all resolve on x86_64 and aarch64 (GNOME 51~beta,
  # `dnf repoquery --forcearch=aarch64`, 2026-08-25) — the measurement
  # hummingbird's arm64 legs skipped (#1755 §3).
  #
  # experimental: true — dispatch-only, no cron. The Q3 flavor freeze
  # (VARIANT-LIFECYCLE.md §1, "no new flavors through 2026-09-30") is a
  # CAPACITY gate, and a nightly EL11 lane is exactly the nightly capacity it
  # protects while 13 variant workflows are still being brought back to
  # green. Dispatch-only adds 0 nightly cells, 0 ISO cells and 0 boot-gate
  # cells. It is not literally zero incremental CI: luks-e2e.yml builds its
  # matrix from build_image with no experimental filter, so wahoo:gnome is one
  # extra boot in the monthly sweep (`0 7 1 * *`) — counted rather than
  # excluded, and pinned by tests/test_matrix_status.py's denominator.
  # Promotion to a cron lane needs the capacity note the gate asks for.
  - id: wahoo
    emoji: "🎏"
    description: "Based on Fedora ELN (EL11 preview, rolling)"
    experimental: true
    base_image: "registry.fedoraproject.org/eln-bootc:latest@sha256:1909ffb6ae0c51c0ac831878f0778d7127f38e8bbfddf90d4a14afb281a05748"
    # No alias: `eln` is Fedora's identifier for a moving development target,
    # and an alias tag is a stability promise this lane deliberately does not
    # make. Revisit if/when ELN gains a versioned publish.
    platforms: ["linux/amd64", "linux/arm64"]
    # Known gap, recorded here because it bounds what this lane may claim:
    # ELN has NO working H.264/H.265 decoder (ffmpeg-free's only h264 entry
    # is backed by the `noopenh264` stub, which encodes 0 bytes; RPM Fusion
    # publishes no ELN branch). See docs/GREEN-MASTER-PLAN.md's wahoo section
    # and the TUNAOS_CODEC_GAP marker in verify-desktop-experience.sh.
    #
    # Scope: every desktop ELN actually composes, and no others. GNOME 51~beta
    # and COSMIC 1.6.0 come from eln-appstream/eln-extras; KDE comes from the
    # `kde-desktop` group (86 entries, Plasma 6.7.4). Each was repodata-measured
    # before its row was added — #858 is an image tagged for a desktop that
    # contains no desktop, and the guard against it is measuring first.
    #
    # Niri and XFCE are deliberately NOT here and must not be added by analogy:
    # ELN ships one package of the Niri stack (`swaybg`) and ZERO `xfce*`
    # packages at all. Both need packages built before a row is honest —
    # tracked in #2051 / #2052 and in tunaos-packages.
    #
    # No ISO and no qcow2 on any flavor — a preview lane publishes images to
    # `bootc switch` onto, not installer media.
    flavors:
      - id: base
        stage: 1
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      # COSMIC 1.6.0 from eln-extras — 23 of 24 manifest packages present
      # (only cosmic-wallpapers absent). Notably needs NO copr and no
      # repo.tunaos.org fprintd repo, both of which the el10 section carries:
      # ELN has fprintd-1.94.5-6.eln158 in eln-appstream (#2049).
      - id: cosmic
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      # Plasma 6.7.4 via the eln-extras `kde-desktop` group; 16 of 23 explicit
      # packages present, the seven misses are peripheral input/security-key
      # extras carried as `optional:` (#2050).
      - id: kde
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false

  - id: bonito
    emoji: "🎣"
    description: "Based on Fedora 44"
    base_image: "quay.io/fedora/fedora-bootc:44@sha256:9c8fae9f47e7287d27542a6fe30ec408151655b142a61beea787c94654f1911a"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [fedora]
    akmods: "ublue-os"
    # Note: linux/amd64/v2 intentionally omitted — verify builder support before adding.
    platforms: ["linux/amd64", "linux/arm64"]
    # Flavor strategy: Fedora ships the latest kernel and GNOME by default,
    # so HWE layers are only provided for gnome (the primary desktop).
    # GNOME 50 is EL10-specific and not needed on Fedora.
    # gnome-nvidia-hwe (stage 4) omitted for now — kernel compatibility TBD.
    flavors:
      # ── Stage 1 ────────────────────────────────────────────────────────────
      - id: base
        stage: 1
        build_image: true
        build_iso: false
        build_qcow2: false

      # ── Stage 2 ────────────────────────────────────────────────────────────
      - id: base-hwe
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: base-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: true
      - id: cosmic
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false

      # ── Stage 3 ────────────────────────────────────────────────────────────
      # HWE: no ISO — install the standard ISO then `bootc switch` to *-hwe
      - id: gnome-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      # Apple Silicon (M1/M2, Asahi Linux) — overlay/asahi.sh; aarch64 only.
      # No ISO: Apple Silicon cannot external-boot; installs go through the
      # asahi-installer path from macOS.
      - id: gnome-asahi
        stage: 3
        platforms: ["linux/arm64"]
        build_image: true
        build_iso: false
        build_qcow2: false
      # Intel Macs with Apple's T2 security chip. Fedora's t2linux stack is
      # x86_64-only; no generic ISO may stand in for this profile because it
      # lacks the T2 kernel. Bootsahi Legacy supplies its dedicated USB flow.
      - id: gnome-t2
        stage: 3
        platforms: ["linux/amd64"]
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: cosmic-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false

  # ── Hummingbird ────────────────────────────────────────────────────────────
  # Fedora Hummingbird: container-native, hardened bootc distribution
  # (Konflux hermetic build model, CKI ARK kernel).
  - id: hummingbird
    emoji: "🐦"
    # Rolling, security-hardened fork tracking Fedora RAWHIDE -- not Fedora 43,
    # whatever the .fc43 dist tags suggest -- and shipping NO desktop upstream.
    # The desktop flavors below are therefore a PORT onto a desktop-less base,
    # not a rebuild of an existing one. Read docs/HUMMINGBIRD.md before
    # reasoning about what its package set should contain.
    description: "Fedora Hummingbird (hardened rolling Rawhide fork, bootc)"
    base_image: "quay.io/hummingbird-community/bootc-os:latest@sha256:c5539f9ed4d93aab6bd41e4f5aef8ab83055f3f9e855a47b69fadb7420d0d1df"
    platforms: ["linux/amd64", "linux/arm64"]
    flavors:
      - id: base
        stage: 1
        build_image: true
        build_iso: true
      # Desktops are amd64-only until the aarch64 rebuild repo converges
      # (#1755 option A, W8 architecture honesty). Measured 2026-08-18:
      # hummingbird/20251124-aarch64 now EXISTS (it 404'd when #1755 was
      # filed) but is a 1358-package seed against x86_64's 8100 — cosmic
      # 8/22 manifest packages present, gnome 5/52, no gnome-shell/gdm and
      # no COSMIC compositor. An arm64 desktop leg today would either 404
      # nothing and install almost nothing (--skip-unavailable), publishing
      # the #858 shape: an image with no desktop. Same treatment as
      # grouper:cosmic's amd64 pin. Re-add "linux/arm64" per flavor when
      # the aarch64 index carries that desktop's manifest set — re-measure
      # with the method in manifests/desktops/cosmic.yaml's hummingbird
      # section comment.
      - id: gnome
        stage: 2
        platforms: ["linux/amd64"]
        build_image: true
        build_iso: true
      # kde and niri are DECLARED but not implemented for hummingbird, and
      # advertising them costs the flavor that is.
      #
      # manifests/desktops/{kde,niri}.yaml have no `packages.hummingbird`
      # section at all -- 0 packages, against gnome's 52 and cosmic's 23.
      # install-desktop.sh routes hummingbird to that section by construction,
      # so both flavors have never had a package set to install. Measured on
      # run 32786338463: the two flavors with no manifest are exactly the two
      # whose image builds failed.
      #
      #   kde   nothing provides libdouble-conversion.so.3 / libfbclient.so.2
      #         needed by qt6-qtbase-*.hum1 from public-hummingbird -- an
      #         UPSTREAM gap; those sonames are in no index, ours or theirs,
      #         and no wave the factory is building supplies them.
      #   niri  TUNAOS_BRANDING_FAIL -- dconf keyfiles written to
      #         /etc/dconf/db/distro.d with no compiled database, because
      #         `dconf update` is guarded on `command -v dconf` and dconf is
      #         not installed when there is no package set to install it.
      #
      # Why this blocks GNOME. build_artifacts_s2 takes `needs: build_stage2`,
      # a matrix job over every stage-2 flavor, so one failing leg skips every
      # ISO in the stage -- gnome's included, however cleanly it built
      # (tuna-os/tunaOS#2059). gnome is the only desktop with a passing
      # installer-smoke cell anywhere in the matrix, so the flavors that were
      # never implemented are blocking the one that works.
      #
      # Re-add each of these the moment it has a `packages.hummingbird`
      # section, exactly as cosmic and gnome do. Nothing here is a judgement
      # about wanting kde or niri on hummingbird; it is a statement that they
      # do not exist yet and should not be advertised as though they do.
      - id: cosmic
        stage: 2
        platforms: ["linux/amd64"]
        build_image: true
        build_iso: true

  # ── Sailfin ──────────────────────────────────────────────────────────────
  # Sailfin Tumbleweed: rolling-release, zypper-based.
  - id: sailfin
    emoji: "🦈"
    description: "Based on openSUSE Tumbleweed (rolling)"
    base_image: "registry.opensuse.org/opensuse/tumbleweed:latest@sha256:1691585b5daf973c032d1f94d3b8b85393d259ac0348c1c5b91abceeeb3334a9"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [opensuse, tumbleweed]
    platforms: ["linux/amd64"]
    flavors:
      # base + gnome carry an arm64 leg solely as the parent chain for
      # gnome-asahi (Tumbleweed is multiarch; bootc comes from zypper).
      # Other DEs stay amd64 until someone asks for them on arm64.
      - id: base
        stage: 1
        platforms: ["linux/amd64", "linux/arm64"]
        build_image: true
      - id: gnome
        stage: 2
        platforms: ["linux/amd64", "linux/arm64"]
        build_image: true
      # Apple Silicon (M1/M2, Asahi Linux) — overlay/asahi.sh, OBS
      # home:mrkcee full stack. aarch64 only; no ISO (asahi-installer path).
      - id: gnome-asahi
        stage: 3
        platforms: ["linux/arm64"]
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: kde
        stage: 2
        build_image: true
      - id: niri
        stage: 2
        build_image: true
      - id: xfce
        stage: 2
        build_image: true
      # COSMIC: newly buildable. Tumbleweed packages the session richly and the
      # whole zypper list installs clean (exit 0), producing cosmic-comp and
      # /usr/share/wayland-sessions/cosmic.desktop — both measured on a real
      # install, not inferred. Note cosmic-settings and cosmic-greeter are NOT
      # packaged on openSUSE; see the manifest for what is used instead.
      - id: cosmic
        stage: 2
        build_image: true

  # ── Guppy ────────────────────────────────────────────────────────────────
  # Guppy: source-based, emerge/portage. Compiles everything from source.
  - id: guppy
    emoji: "🌈"
    description: "Based on Gentoo Linux (source-based)"
    # Digest-pinned to match Containerfile.gentoo's FROM exactly. This value
    # is not decorative: reusable-build-image.yml pre-pulls it to warm local
    # storage before the build (a workaround for quay/docker CDN blob drops,
    # #675). With a bare :latest here it warmed whatever latest happened to be
    # while the Containerfile built from the pinned digest, so the pre-pull
    # fetched an image the build never used and the workaround silently did
    # nothing for guppy. Keep the two in lockstep when bumping.
    base_image: "docker.io/gentoo/stage3:latest@sha256:9d03b48eda632da09f469d384ce77ec05618060ed4e9ac295f8192a196e7539a"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [gentoo]
    platforms: ["linux/amd64"]
    flavors:
      - id: base
        stage: 1
        build_image: true
      - id: gnome
        stage: 2
        build_image: true
      - id: kde
        stage: 2
        build_image: true
      # XFCE: every atom in the emerge list was verified to exist in the Gentoo
      # main tree before this was declared. The list previously named
      # xfce-base/lightdm, which does not exist (it is x11-misc/lightdm) — so
      # declaring this flavor earlier would have failed on an unresolvable atom.
      - id: xfce
        stage: 2
        build_image: true
      # NOT declared, and not an oversight:
      #   niri    — no ebuild in the Gentoo main tree at all. A search returns
      #             only dev-ruby/niceogiri. Declaring it would publish an
      #             installable image with no compositor, exactly as
      #             flounder:niri did (tunaOS#915). It IS packaged in the
      #             community GURU overlay (gui-wm/niri, ~amd64) — see
      #             manifests/desktops/niri.yaml for the full finding. Not
      #             enabled here: an untested source build, and ~amd64
      #             keywording that can shift under guppy without warning.
      #   cosmic  — same: no cosmic-comp or cosmic-session ebuild in the tree,
      #             and cosmic.yaml has no emerge section.
      # Both measured and tracked in tunaOS#923.
      # Do not add either until the packages exist; the emerge path now fails
      # loudly on an empty list rather than shipping a desktop-less image.

  # ── Rawhide ────────────────────────────────────────────────────────────────
  # Fedora Rawhide (rolling development branch). Uses the same Containerfile
  # as bonito — Fedora bootc with the latest kernel and packages.
  - id: bonito-rawhide
    publish_name: bonito
    tag_suffix: rawhide
    emoji: "🐉"
    description: "Based on Fedora Rawhide (rolling)"
    base_image: "quay.io/fedora/fedora-bootc:rawhide@sha256:6c04d2602182c7f8ae465be9ecf9987695e26895a35f7226dbe8b63be912d5c3"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [fedora]
    akmods: "ublue-os"
    platforms: ["linux/amd64", "linux/arm64"]
    flavors:
      - id: base
        stage: 1
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: base-hwe
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: base-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 2
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: true
      - id: cosmic
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce
        stage: 2
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: gnome-hwe
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: gnome-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: cosmic-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: niri-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce-nvidia
        # x86_64 only: the akmods-nvidia-open kmods are x86_64 builds, so on
        # any other platform the exact-EVR kmod install can never match and
        # the overlay hard-fails (overrides/nvidia/10-kernel-swap.sh) rather
        # than shipping a driverless "nvidia" image. Also excludes amd64/v2:
        # Kitten v2 kernels are ARCH=x86_64_v2, which no published kmod
        # matches. Same shape as bluefin's x86_64-only GDX.
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false

  # ── Ubuntu ───────────────────────────────────────────────────────────────
  # RFC 010: Ubuntu 26.04 variant. x86_64-only.
  # Desktops: gnome/kde/niri/xfce via the apt branches in build_scripts/*.sh
  # (cosmic has no apt branch yet). No HWE/NVIDIA layers — Ubuntu ships its
  # own kernel stack and the akmods images are RPM-only.
  # ── elementary / Pantheon ──────────────────────────────────────────
  # A SEPARATE variant rather than a grouper flavour, and the base image is
  # why. Pantheon is absent from the Ubuntu archive entirely (verified on
  # ubuntu:resolute with main+universe+restricted+multiverse enabled: gala,
  # wingpanel, pantheon-session and switchboard all ABSENT, only `plank`
  # present). It comes solely from ppa:elementary-os/stable, which publishes
  # for **noble and jammy only** — nothing for resolute. grouper is Ubuntu
  # 26.04, so Pantheon cannot be installed there at any version today.
  #
  # Revisit collapsing this into grouper when elementary publishes for 26.04.
  - id: gurnard
    emoji: "🐟"
    description: "Based on Ubuntu 24.04 Noble Numbat with the Pantheon desktop"
    base_image: "docker.io/library/ubuntu:noble@sha256:561618e2c15bf2397621dd04f96926663a3b5616c189cf7e38db7e82f5c538ea"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [elementary]
    platforms:
      - linux/amd64
      - linux/arm64
    flavors:
      - id: base
        stage: 1
        build_image: true
      - id: pantheon
        stage: 2
        build_image: true
        build_iso: true

  - id: grouper
    emoji: "🐟"
    description: "Based on Ubuntu 26.04 Resolute Raccoon"
    base_image: "docker.io/library/ubuntu:resolute@sha256:678c6550cc43645e08669028bc177f50be4e7c5b8cca677067b1914d4afc7a03"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [ubuntu]
    # Built directly from stock Ubuntu and bootcified in Containerfile.ubuntu
    # (bootc from the tuna-os bootc-toolchain-sid oras artifact). See RFC 010.
    # arm64: toolchain publishes latest-{amd64,arm64}; Containerfile.ubuntu is
    # TARGETARCH-aware. Apple Silicon (Asahi) support gated behind
    # ENABLE_ASAHI=1 (build_scripts/ubuntu-kernel.sh) — flavor wiring TBD.
    platforms: ["linux/amd64", "linux/arm64"]
    flavors:
      - id: base
        stage: 1
        build_image: true
      - id: gnome
        stage: 2
        build_image: true
        build_iso: true
      # Apple Silicon (M1/M2, Asahi Linux) — full build with ENABLE_ASAHI=1
      # (the kernel decision lives in Containerfile.ubuntu's base stage, so
      # this is not an overlay). aarch64 only; no ISO (asahi-installer path).
      #
      # stage 3, NOT stage 1, and the distinction matters: `stage` is build
      # ORDER, not parentage. This flavor has no PARENT_FLAVOR, so it still
      # builds straight from base wherever it is placed — but at stage 1 it
      # shared a matrix with base, and a stage does not advance until every
      # cell in it passes. One arm64-only Apple Silicon flavor therefore gated
      # gnome, kde, gnome-zfs, cosmic and xfce, which all live in stage 2:
      # run 30688732984 promoted base and then never started them.
      #
      # Every other variant already places gnome-asahi at stage 3; grouper was
      # the lone exception, because there asahi is a full build rather than an
      # overlay and the stage number was read as "has no parent" rather than
      # "runs first".
      - id: gnome-asahi
        stage: 3
        platforms: ["linux/arm64"]
        build_image: true
        build_iso: false
      # GNOME with ZFS-root support (prebuilt zfs.ko + zfs-dracut). ISO gated
      # off until the install-to-filesystem ZFS-root path has a boot e2e (#625).
      - id: gnome-zfs
        stage: 2
        build_image: true
        build_iso: false
      - id: kde
        stage: 2
        build_image: true
        build_iso: true
      # No niri — same reason as flounder and flounder-sid (#918): niri is not
      # packaged for apt in Debian trixie, Debian sid or Ubuntu 26.04, and
      # manifests/desktops/niri.yaml has no apt section. grouper is Ubuntu, so
      # it was covered by that evidence but missed by the change.
      #
      # Nothing would have caught it either. With no packages.apt section the
      # installer skips the install without error, and the apt path `exit 0`s
      # before the shared tail — so the desktop experience contract never runs
      # on an apt variant at all. That is the mechanism that published
      # flounder:niri with no compositor (tunaOS#915); grouper:niri is not in
      # the published set, so it evidently never completed, but the
      # declaration was wrong either way.
      # Packaging it ourselves is tunaos-packages#136. Do not re-add without it.
      #
      # COSMIC, by contrast, IS buildable here and is now declared. Ubuntu has
      # no COSMIC in its own repos, but cosmic.yaml carries
      # ppa:hepp3n/cosmic-epoch with condition: ubuntu, and install-desktop.sh
      # adds a PPA when IS_UBUNTU — measured WITH the PPA enabled,
      # cosmic-session, cosmic-comp, cosmic-settings and cosmic-greeter all
      # resolve. (Measuring in a bare ubuntu container says nothing here: the
      # installer adds repositories before it installs, so a probe that skips
      # that step is answering a question about the wrong system.)
      # amd64 only: ppa:hepp3n/cosmic-epoch publishes amd64 and amd64v3 and
      # nothing else — no arm64 build of cosmic-session/cosmic-comp/etc exists
      # for any series (Launchpad getPublishedBinaries). grouper otherwise
      # builds both arches, so the arm64 job was the ONLY failure in run
      # 31088031074 while amd64 built clean. Same shape as the aarch64 cosmic
      # pin on the EL10 variants for fprintd-pam (tunaOS#732).
      #
      # This PPA dependency is tracked for removal in tunaOS#964 — see the
      # status note at the top of manifests/desktops/cosmic.yaml's apt:
      # block for exactly what has and hasn't landed on the Tideforge side.
      - id: cosmic
        stage: 2
        platforms: ["linux/amd64"]
        build_image: true
      - id: xfce
        stage: 2
        build_image: true
        build_iso: true

  # ── Arch Linux ─────────────────────────────────────────────────────────────
  # Marlin: rolling-release Arch Linux on bootc. Uses Containerfile.arch
  # which builds bootc from source (Arch doesn't package it yet) and sets
  # up the ostree/composefs filesystem layout.
  - id: marlin
    emoji: "🚀"
    description: "Based on Arch Linux (rolling)"
    base_image: "docker.io/archlinux/archlinux:latest@sha256:71810bb6f8bece3e2172e458dd8c0793fec139a6b6a7e5da2c832f96d1b9a0a6"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [arch, archlinux]
    # arm64 legs use ghcr.io/tuna-os/archlinuxarm instead (docker.io/archlinux
    # is x86_64-only) — see get-base-image.sh + build-archlinuxarm-base.yml
    # (#778). Only the gnome-asahi parent chain builds arm64.
    platforms: ["linux/amd64"]
    flavors:
      - id: base
        stage: 1
        platforms: ["linux/amd64", "linux/arm64"]
        build_image: true
      - id: gnome
        stage: 2
        platforms: ["linux/amd64", "linux/arm64"]
        build_image: true
        build_iso: true
      # gnome-asahi: NOT declared, and not an oversight.
      #
      # The Asahi-ALARM repo's linux-asahi package is real (verified by
      # downloading it directly: asahi-alarm/asahi-alarm release "aarch64",
      # linux-asahi-7.1.5.asahi2-1-aarch64.pkg.tar.xz), but it ships only 5
      # T8103 (M1) device trees — nowhere near the ~50 the asahi-verify
      # harness requires for real hardware coverage — and is missing several
      # Apple SoC kernel modules the harness checks for, most importantly
      # asahi.ko itself (the GPU driver; appledrm.ko, the older
      # display-only driver, IS present). The published
      # ghcr.io/tuna-os/marlin:gnome-asahi tag scored 23/36 on the harness
      # (run 31243787771, 2026-08-08) — an ordinary arm64 image with a
      # differently-named kernel, not a bootable Apple Silicon image.
      #
      # tunaOS#911: "until fixed, [this] should stop being promoted rather
      # than shipping as unbootable" — a user selecting it in the installer
      # catalog gets a Mac that does not boot. Re-add once asahi-alarm's
      # linux-asahi actually ships the missing modules and device trees, or
      # once TunaOS has its own source of a complete Apple Silicon kernel
      # for Arch. Same pattern as guppy's absent niri/cosmic flavors above.
      - id: kde
        stage: 2
        build_image: true
        build_iso: true
      - id: cosmic
        stage: 2
        build_image: true
      - id: niri
        stage: 2
        build_image: true
      - id: xfce
        stage: 2
        build_image: true
      # CachyOS kernel overlay on the Arch base — like -hwe/-nvidia elsewhere
      # (Containerfile.overlay + build_scripts/cachyos.sh, stage 3 on the parent DE).
      - id: gnome-cachyos
        stage: 3
        build_image: true
      - id: kde-cachyos
        stage: 3
        build_image: true
      - id: cosmic-cachyos
        stage: 3
        build_image: true
      - id: niri-cachyos
        stage: 3
        build_image: true
      - id: xfce-cachyos
        stage: 3
        build_image: true
      # NVIDIA overlay on the Arch base (tunaOS#624): nvidia-open-dkms via
      # pacman + dkms, not akmods — Arch has no RPM/akmods concept at all.
      # See build_scripts/overlay/overrides/nvidia-arch/20-nvidia.sh. No
      # *-nvidia-hwe: marlin has no HWE kernel layer to begin with (Arch
      # ships the latest kernel natively), so there is nothing to stack an
      # nvidia overlay on top of beyond plain *-nvidia.
      #
      # platforms is pinned to linux/amd64 explicitly (not inherited from
      # the variant default) on every *-nvidia flavor in this file — a
      # test (tests/bats/test_nvidia_overlay.bats) asserts that directly,
      # and nvidia-open-dkms/nvidia-utils only publish x86_64 builds.
      - id: gnome-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: cosmic-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: niri-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: xfce-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false

  # ── Debian ─────────────────────────────────────────────────────────────────
  # Flounder: Debian Trixie (13) stable. Uses Containerfile.debian.
  - id: flounder
    emoji: "🐡"
    description: "Based on Debian 13 Trixie (stable)"
    base_image: "docker.io/library/debian:trixie@sha256:34cd9e9fd437c0a095ec39cb2e73422c9f30821b0d0848ed74fd0d43bae4d958"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [debian]
    platforms: ["linux/amd64"]
    flavors:
      # base + gnome carry an arm64 leg solely as the parent chain for
      # gnome-asahi (debian:trixie is multiarch; the bootc toolchain
      # publishes latest-arm64). Other DEs stay amd64 for now.
      - id: base
        stage: 1
        platforms: ["linux/amd64", "linux/arm64"]
        build_image: true
      - id: gnome
        stage: 2
        platforms: ["linux/amd64", "linux/arm64"]
        build_image: true
        build_iso: true
      # gnome-asahi: NOT declared, and not an oversight.
      #
      # build_scripts/overlay/asahi.sh's debian branch resolves a real
      # linux-image-*-asahi package from the Bananas team's archive, but the
      # published ghcr.io/tuna-os/flounder:gnome-asahi tag currently ships
      # stock 6.12.100+deb13-arm64 — the purge-then-install of the asahi
      # kernel never runs, because it is not even reached: this flavor's
      # arm64 build fails outright at Containerfile.overlay's `pacman`
      # detection step. That step uses bash-only `command -v pacman
      # &>/dev/null` in a RUN instruction with no `bash -c` wrapper, so it
      # runs under the image's default shell — dash on this (Debian) base,
      # which does not understand `&>` and mis-parses it as `command -v
      # pacman &` (backgrounded) `>/dev/null` (redirecting nothing), always
      # entering the `then` branch and invoking a `pacman` that does not
      # exist on Debian (build 93057902527, 2026-08-08: "pacman: not
      # found", exit 127). Fixed in this same change (Containerfile.overlay
      # now uses portable redirection), but with that fixed the harness
      # still needs to confirm the Bananas kernel actually boots and covers
      # real Apple Silicon hardware before this is safe to publish again —
      # unverified as of this writing.
      #
      # tunaOS#911: "until fixed, [this] should stop being promoted rather
      # than shipping as unbootable" — a user selecting it in the installer
      # catalog gets a Mac that does not boot. Re-add once a build with the
      # portability fix has actually produced and verified an asahi kernel
      # here. Same pattern as guppy's absent niri/cosmic flavors elsewhere
      # in this file.
      - id: kde
        stage: 2
        build_image: true
        build_iso: true
      # No cosmic: COSMIC is not packaged in Debian AT ALL. Checked every
      # suite via ftp-master's madison API (oldoldstable through unstable and
      # experimental) for cosmic-session, cosmic-comp, cosmic-panel,
      # cosmic-files, cosmic-term, cosmic-settings, cosmic-greeter and
      # xdg-desktop-portal-cosmic — every one returns empty, while a control
      # (kde-plasma-desktop) returns suites. The apt section of
      # manifests/desktops/cosmic.yaml is served by ppa:hepp3n/cosmic-epoch,
      # which is `condition: ubuntu` and publishes for resolute only, so on a
      # Debian base the package list has no source at all:
      #   E: Unable to locate package cosmic-comp   (and 18 more)
      # flounder:cosmic, LUKS run 31087927879. Same reason niri is absent
      # below; it fails louder only because apt exits 100 on an unknown name
      # while niri.yaml had no apt section at all, so that one installed
      # nothing and exited 0. Do not re-add without a Debian source for COSMIC.
      #
      # That Debian source is what tunaOS#964 is building — our own
      # Tideforge-built debs, not another PPA (which wouldn't cover Debian
      # anyway). See manifests/desktops/cosmic.yaml's apt: block for the
      # current widen/publish status before re-adding this flavor.
      # No niri: it is not packaged for apt in Debian trixie, Debian sid or
      # Ubuntu 26.04 (all three checked), and manifests/desktops/niri.yaml
      # has no apt section as a result. Building it produced a PUBLISHED,
      # installable image with no compositor at all — `command -v niri` was
      # empty in ghcr.io/tuna-os/flounder:niri. See tunaOS#915; packaging it
      # ourselves is tracked in tunaos-packages#136 and needs a whole deb
      # toolchain this org does not have yet. Do not re-add without one.
      - id: xfce
        stage: 2
        build_image: true
      # NVIDIA overlay on the Debian base (tunaOS#624): nvidia-driver via
      # apt (official Debian non-free component, not a third-party repo) +
      # dkms. See build_scripts/overlay/overrides/nvidia-debian/20-nvidia.sh.
      # No *-nvidia-hwe: flounder has no HWE kernel layer (Debian ships one
      # kernel per suite), so there is nothing to stack an nvidia overlay on
      # top of beyond plain *-nvidia.
      #
      # platforms is pinned to linux/amd64 explicitly on every *-nvidia
      # flavor in this file — asserted by
      # tests/bats/test_nvidia_overlay.bats — matching nvidia-driver's own
      # Debian archive coverage focus and this variant's amd64-only DE set.
      - id: gnome-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: kde-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: true
        build_qcow2: false
      - id: xfce-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false

  # Flounder-sid: Debian Sid (unstable/rolling).
  - id: flounder-sid
    publish_name: flounder
    tag_suffix: sid
    emoji: "☢️"
    description: "Based on Debian Sid (unstable, rolling)"
    base_image: "docker.io/library/debian:sid@sha256:24c766c06ebdbda66a026e92361ede7d9212819701b2fca82e347ef0c240cb2c"
    # Friendly alias: same digest also published as ghcr.io/tuna-os/<alias>:<flavor>
    aliases: [debian]
    platforms: ["linux/amd64"]
    flavors:
      - id: base
        stage: 1
        build_image: true
      - id: gnome
        stage: 2
        build_image: true
      - id: kde
        stage: 2
        build_image: true
      # No cosmic — same reason as flounder above. The header on this variant
      # used to read "Has latest COSMIC natively"; it does not. sid returns
      # empty from madison for every cosmic binary, exactly like trixie.
      # No niri — same reason as flounder above (absent from Debian sid too).
      - id: xfce
        stage: 2
        build_image: true
      # NVIDIA overlay on the Debian sid base (tunaOS#624) — same mechanism
      # as flounder above (apt non-free + dkms). No *-nvidia-hwe: sid has
      # no HWE kernel layer, same reasoning as flounder. No ISOs: this
      # variant does not build any today (rolling/unstable), so none of its
      # base DE flavors set build_iso either — nvidia flavors match that.
      #
      # platforms is pinned to linux/amd64 explicitly on every *-nvidia
      # flavor in this file — asserted by
      # tests/bats/test_nvidia_overlay.bats.
      - id: gnome-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: kde-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
      - id: xfce-nvidia
        platforms: ["linux/amd64"]
        stage: 3
        build_image: true
        build_iso: false
        build_qcow2: false
