name: Build Redfin
on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:
    inputs:
      flavor:
        description: 'Flavor (all, base, gnome, kde, niri, etc.)'
        default: 'all'
        type: string

concurrency:
  group: build-redfin-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: 🔒-redfin
    uses: ./.github/workflows/build-variant.yml
    with:
      variant: 'redfin'
      flavor: ${{ inputs.flavor || 'all' }}
    secrets: inherit
