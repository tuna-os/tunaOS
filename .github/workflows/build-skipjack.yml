name: Build Skipjack
on:
  schedule:
    - cron: "0 5 * * *"  # 05:00 UTC daily (staggered from albacore)
  workflow_dispatch:
    inputs:
      flavor:
        description: 'Flavor (all, base, gnome, kde, niri, etc.)'
        default: 'all'
        type: string

concurrency:
  group: build-skipjack-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: 🍣-skipjack
    uses: ./.github/workflows/build-variant.yml
    with:
      variant: 'skipjack'
      flavor: ${{ inputs.flavor || 'all' }}
    secrets:
      SIGNING_SECRET: ${{ secrets.SIGNING_SECRET }}
